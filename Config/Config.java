package Config;

public interface Config {
    int DISK_CAPACITY = 100*1000*1000; // We set the disk size to be 100MB
    int BLOCK_SIZE_200 = 200; // We set the block size to be 200B
    String DATA_FILE_PATH = "data.tsv"; //declare the path
}
